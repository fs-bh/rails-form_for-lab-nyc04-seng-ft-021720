<h1>All Students</h1>
<style>ol{list-style:none;}ol>li:before{content:attr(seq)'. ';}</style>
<ol>
	<% @students.each do |student| %>
		<li seq='<%= student.id %>'><%= link_to student.to_s, student_path(student) %></li>
	<% end %>
</ol>
<%= link_to 'Create New Student', new_student_path %>